{
  "name": "@c3-oss/prosa-webp-widgets-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Developer tooling for prosa-webp-widgets.",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "commitlint": "commitlint --config commitlint.config.cjs",
    "lint:links": "just lint-links",
    "lint:md": "just lint-md",
    "lint:staged": "lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  }
}
